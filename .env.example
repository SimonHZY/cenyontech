PORT=3000

# SMTP server settings
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SECURE=true

# Optional: if your SMTP relay requires login, fill these two values.
SMTP_USER=
SMTP_PASS=

# Mail sender and receiver
MAIL_FROM=sender@example.com
MAIL_TO=market@cenyontech.com
